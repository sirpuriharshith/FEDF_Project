import PageShell from '../../components/PageShell';
import Card from '../../components/Card';
import { firstAid } from '../../services/data';
export default function Emergency(){ const nums=[['Ambulance (India)','102'],['All-in-one Emergency','112'],['Poison Control','1066'],['Mental Health iCall','+91 9152987821']]; return <PageShell title="Emergency Guide" subtitle="Breathe. You're in the right place. Tap a number to call." wide><section className="grid four emergency-numbers">{nums.map(n=><a className="emergency-card" href={`tel:${n[1]}`} key={n[1]}><span>☎</span><small>{n[0]}</small><b>{n[1]}</b></a>)}</section><section className="quick-actions"><button className="primary">📍 Share Live Location</button><button className="primary">📞 Call Emergency Contact</button><button className="primary">🏥 Nearest Hospital</button></section><h2>First-response checklists</h2><section className="grid two">{firstAid.map(f=><Card key={f.title}><h3>▿ {f.title}</h3><ol>{f.steps.map(s=><li key={s}>{s}</li>)}</ol><b className="red">AVOID</b><ul>{f.avoid.map(a=><li key={a}>{a}</li>)}</ul></Card>)}</section></PageShell> }
